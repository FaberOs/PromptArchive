{
  "name": "prompt-archive",
  "version": "1.2.0",
  "private": true,
  "description": "Prompt Archive - Biblioteca de prompts para IA",
  "scripts": {
    "doctor": "node scripts/run-react-doctor.mjs --human",
    "doctor:strict": "node scripts/run-react-doctor.mjs --strict",
    "doctor:json": "node scripts/run-react-doctor.mjs --json",
    "doctor:app": "node scripts/run-react-doctor.mjs"
  },
  "devDependencies": {
    "react-doctor": "0.9.4"
  }
}
